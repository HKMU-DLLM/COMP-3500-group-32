<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<title>Edit Menu Item</title>
	</head>
	<body>
		<h1>Edit Menu Item</h1>

		<form action="/restaurant/menu/edit/<%= menu.id %>" method="post">
			<input type="hidden" name="restaurant" value="<%= menu.restaurant %>" />

			<label>Dish Name: </label>
			<input
				type="text"
				name="dish_name"
				value="<%= menu.dish_name %>"
				required /><br /><br />

			<label>Price: </label>
			<input
				type="number"
				name="price"
				value="<%= menu.price %>"
				step="0.01"
				required /><br /><br />

			<button type="submit">Save Changes</button>
			<button
				type="button"
				onclick="window.location.href='/restaurant?name=<%= encodeURIComponent(menu.restaurant) %>'">
				Cancel
			</button>
		</form>
	</body>
</html>
